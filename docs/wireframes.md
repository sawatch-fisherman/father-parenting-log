# TotoOps MVP 画面詳細レイアウト（ワイヤーフレーム）

> 本ドキュメントは、[screens.md](screens.md) で確定した画面一覧（S1〜S13）・ルーティング・Controller構成を前提に、**各画面内のコントロール配置（ボタン・入力欄・タブなど）のみ**を決めるものです。色・余白・フォント・アイコン意匠などの視覚デザインは対象外です。
>
> 4×2アイコングリッド（S3・S9）／期間タブ＋集計（S12）／日付グループのタイムライン（S13）は、参考として類似アプリ（Studyplus）のスクリーンショットを参照して構成しています（参考画像はローカル専用ファイルのためリポジトリには含めていません）。

## 記法凡例

| 記法 | 意味 |
|---|---|
| `[ボタン]` | ボタン |
| `(入力欄)` | テキスト入力 |
| `<セレクト ▾>` | ドロップダウン／セレクト |
| `《タブA｜タブB》` | タブ切り替え |
| `┌─┐│└─┘` | カード／グリッドの枠線 |
| `…` | ケバブメニュー（その他操作） |
| `>` | タップで詳細・次画面へ遷移することを示す行末記号 |

---

## S1 ログイン画面

```text
┌─────────────────────────────┐
│                             │
│           TotoOps           │
│   父親の育児参加を記録する   │
│                             │
│      [ Googleでログイン ]    │
│                             │
└─────────────────────────────┘
```

- Google SSOボタンのみ。メール／パスワード欄は持たない。

---

## S2 プロフィール登録画面

```text
┌─────────────────────────────┐
│  プロフィール登録            │
├─────────────────────────────┤
│ ニックネーム（必須）         │
│ (                         ) │
│                             │
│ 年代（任意）                │
│ <未選択                  ▾> │
│                             │
│ 子どもの年齢帯（任意）       │
│ <未選択                  ▾> │
│                             │
│          [ 登録する ]        │
└─────────────────────────────┘
```

- 初回ログイン時のみ表示。戻るボタン・グローバルナビなし。登録完了でS3へ遷移。

---

## S3 記録画面

```text
┌─────────────────────────────┐
│ 👤            記録する       │
├─────────────────────────────┤
│ ┌──────┬──────┬──────┬──────┐│
│ │おむつ │ミルク │お風呂 │寝かし││
│ │交換  │      │      │つけ  ││
│ ├──────┼──────┼──────┼──────┤│
│ │夜泣き │着替え │歯磨き │送迎  ││
│ │対応  │      │      │      ││
│ └──────┴──────┴──────┴──────┘│
│ （4列×2段・短タップ＝即記録／ │
│   長押し＝S10で日時指定）     │
│                             │
│         [   その他   ]       │
├─────────────────────────────┤
│  記録 │ 履歴 │ 集計 │ 設定    │
└─────────────────────────────┘
```

- ヘッダーのユーザーアイコンはGoogle SSOから取得表示のみ（DB非保存）。設定へはグローバルナビ経由。
- グリッド内のラベルは例示（features.mdの候補17種から代表例）。実際にピン留めする共通の初期8個は未決（decisions.md §2 #11）。

---

## S4 「その他」イベント選択画面

```text
┌─────────────────────────────┐
│ [← 戻る]   その他のイベント   │
├─────────────────────────────┤
│  夜泣き対応               >  │
│  ─────────────────────────  │
│  離乳食                   >  │
│  ─────────────────────────  │
│  爪切り                   >  │
│  ─────────────────────────  │
│  病院付き添い              >  │
│  ─────────────────────────  │
│  （ピン留めされていない候補） │
└─────────────────────────────┘
```

- グローバルナビなし。各行タップで必ずS10（実施日時指定画面）へ遷移し、即記録は行わない。

---

## S5 称号獲得モーダル

```text
┌─────────────────────────────┐
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░ │ ← S3上の半透明オーバーレイ
│  ┌───────────────────────┐  │
│  │        🏅             │  │
│  │   称号を獲得しました！  │  │
│  │                       │  │
│  │  「おむつ交換士 Lv.5」  │  │
│  │                       │  │
│  │ [ Xに投稿 ]  [ 閉じる ] │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

- 育児イベント登録の結果として自動表示（ユーザー操作による遷移ではない）。「閉じる」でS3に戻る。

---

## S6 X投稿文生成モーダル

```text
┌─────────────────────────────┐
│  ┌───────────────────────┐  │
│  │ 🏅称号を獲得しました！   │  │
│  │                       │  │
│  │ 「おむつ交換士 Lv.5」   │  │
│  │ 累計おむつ交換：100回。 │  │
│  │ 父親育児、今日も運用中。 │  │
│  │ #TotoOps #父親育児     │  │
│  │                       │  │
│  │[コピー][Xを開く][閉じる]│  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

- S5から遷移。テキストはS5のデータからクライアント側で生成（サーバー往復なし）。コピー後閉じるとS3へ戻る。

---

## S7 設定画面（ハブ）

```text
┌─────────────────────────────┐
│           設定               │
├─────────────────────────────┤
│  プロフィール編集          > │
│  ─────────────────────────  │
│  ピン留め設定              > │
│  ─────────────────────────  │
│  （卒業）※Phase 2           │
│  （全体集計）※Phase 2       │
│  （広告）※Phase 2           │
│  ─────────────────────────  │
│         [ ログアウト ]       │
├─────────────────────────────┤
│  記録 │ 履歴 │ 集計 │ 設定    │
└─────────────────────────────┘
```

- Phase 2以降の導線は器として用意（未活性表現で配置のみ）。

---

## S8 プロフィール編集画面

```text
┌─────────────────────────────┐
│ [← 戻る]  プロフィール編集    │
├─────────────────────────────┤
│ ニックネーム                 │
│ (現在の値                 ) │
│                             │
│ 年代                        │
│ <現在の値                ▾> │
│                             │
│ 子どもの年齢帯               │
│ <現在の値                ▾> │
│                             │
│          [ 保存する ]        │
└─────────────────────────────┘
```

- 閲覧専用画面は別途用意せず、この画面が閲覧を兼ねる。保存でS7へ戻る。

---

## S9 ピン留め設定画面

```text
┌─────────────────────────────┐
│ [← 戻る]  ピン留め設定        │
├─────────────────────────────┤
│  現在の8枠                  │
│ ┌──────┬──────┬──────┬──────┐│
│ │①おむつ│②ミルク│③お風呂│④寝か││
│ │交換[⇄]│  [⇄] │  [⇄] │し[⇄]││
│ ├──────┼──────┼──────┼──────┤│
│ │⑤夜泣き│⑥着替え│⑦歯磨き│⑧送迎││
│ │  [⇄] │  [⇄] │  [⇄] │  [⇄]││
│ └──────┴──────┴──────┴──────┘│
│                             │
│  候補一覧（未使用の種別）     │
│  離乳食 / 爪切り / 病院付き添い│
│  ...                        │
│                             │
│          [ 保存する ]        │
└─────────────────────────────┘
```

- `[⇄]` は入れ替え操作（候補一覧から選んで差し替えるイメージ）。保存でS7へ戻る。

---

## S10 実施日時指定画面

```text
┌─────────────────────────────┐
│ [← 戻る]   おむつ交換         │
├─────────────────────────────┤
│ 実施日                       │
│ ( 2026-07-15              ) │
│                             │
│ 実施時刻                     │
│ ( 21:30                   ) │
│                             │
│ メモ（任意）                 │
│ (                         ) │
│                             │
│          [ 保存する ]        │
└─────────────────────────────┘
```

- ヘッダーには選択済みのイベント種別名を表示のみ。S3の長押し、またはS4の項目タップから遷移。
- 保存後、称号条件達成ならS5へ、未達成ならS3へ遷移。

---

## S11 ログ編集画面

```text
┌─────────────────────────────┐
│ [← 戻る]   ログ編集           │
├─────────────────────────────┤
│ イベント種別（変更不可）      │
│  おむつ交換                  │
│                             │
│ 実施日                       │
│ ( 2026-07-15              ) │
│                             │
│ 実施時刻                     │
│ ( 21:30                   ) │
│                             │
│   [ 保存する ]   [ 削除する ] │
└─────────────────────────────┘
```

- S13の各行「…」から遷移。`occurred_at` のみ変更可能。種別を変えたい場合は削除して S3/S4 から再記録する。

---

## S12 期間別集計画面

```text
┌─────────────────────────────┐
│           集計               │
├─────────────────────────────┤
│ 《日｜週｜月｜全期間》         │
│                             │
│  [ < ]  2026/07/09〜15 [ > ] │
│                             │
│ ┌───────────────────────┐   │
│ │        （グラフ）       │   │
│ └───────────────────────┘   │
│                             │
│  種別ごとの件数内訳          │
│  おむつ交換    12回          │
│  ミルク        8回           │
│  お風呂        5回           │
│  ...                        │
├─────────────────────────────┤
│  記録 │ 履歴 │ 集計 │ 設定    │
└─────────────────────────────┘
```

- 「全期間」タブでは期間送り矢印を非表示にし、自分の累計実績として表示する（Phase 2で称号一覧画面への導線を追加予定）。

---

## S13 記録履歴画面（タイムライン）

```text
┌─────────────────────────────┐
│           履歴               │
├─────────────────────────────┤
│ 2026年7月15日                │
│  21:30 おむつ交換        … │
│  ───────────────────────── │
│  19:00 お風呂            … │
│                             │
│ 2026年7月14日                │
│  23:10 夜泣き対応        … │
│  ───────────────────────── │
│  ...                        │
├─────────────────────────────┤
│  記録 │ 履歴 │ 集計 │ 設定    │
└─────────────────────────────┘
```

- 新しい順、日付ごとにグループ化。各行の「…」タップでS11へ遷移。
