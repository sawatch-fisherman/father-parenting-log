# Design Guidelines

> TotoOps（開発名。前身 DadOps）のビジュアルデザイン指針。`docs/concept.md`・`docs/features.md`・`docs/screens.md`・`docs/wireframes.md`・`docs/data-model.md`・`docs/privacy.md`・CLAUDE.md の内容と矛盾しないことを前提に、独自に設計したもの。
> 複数の公開デザイン資料から一般化した原則は `.claude/references/design/notes/extracted-design-principles.md` を参照。本書は、それらの原則と本プロジェクトの要件を踏まえて策定した**TotoOps専用のデザイン方針**である。
> 本書は「これから作る」ための設計図であり、既存実装を追認したものではない。M1・M2 で実装した画面（S1ログイン／S2プロフィール登録／S8プロフィール編集）には本書の色・タイポグラフィ・コンポーネント仕様を適用済み。M3 以降の画面（記録・履歴・集計・設定）とグローバルナビ・トースト・モーダルは未実装。

---

## 1. Purpose

TotoOpsは、父親が自分自身の育児行動を記録し、自分の貢献を振り返るためのWebアプリである。記録すること自体が目的であり、他者（母親・他の父親・他地域）と比較・競争させることは目的ではない。

DESIGN.mdの目的は、この「振り返りのための静かな記録帳」という体験を、色・文字・余白・コンポーネントという具体的な実装レベルの決定に翻訳し、今後この画面・あの画面で見た目がバラバラになる、あるいは無自覚に「比較・競争・叱責」を連想させるUIが混入する、といった事態を防ぐことにある。

## 2. Target Users

- **主利用者**：乳幼児を育てる父親。育児経験の有無、ITリテラシーは問わない。
- **利用シーン**：日中の隙間時間だけでなく、深夜の授乳・夜泣き対応・おむつ替えの直後など、**疲れていて片手しか使えない状況**での利用を強く想定する。
- **心理的な特徴**：「もっと頑張れるはず」という自己否定や、パートナーとの比較で追い詰められている可能性がある利用者も含む。デザインは常に**責めない・急かさない**トーンを保つ。
- **利用デバイス**：スマートフォンが主。PCはS12（集計）やS7（設定）などをじっくり見る場面で使われる程度と想定する（詳細は9章）。

## 3. Design Principles

抽出した一般原則（`.claude/references/design/notes/extracted-design-principles.md`）のうち、TotoOpsにとって特に重要な4つを行動原則として掲げる。

1. **迷わせない、押し間違えさせない**：疲れた状態での片手操作を前提に、タップ領域は大きく、主要操作と補助操作は塗り分けで明確に区別する（5章・10章）。
2. **責めない、急かさない**：エラー・リマインド・空状態のすべてにおいて、色もコピーも「次にどうすればいいか」を穏やかに伝える。赤い警告バナーで詰め寄るような表現は使わない（11章・14章・15章）。
3. **色の意味を薄めない**：ブランドを象徴する色（Primary＝緑）と状態を伝える色（Success/Warning/Error/Info）の色相を明確に分離し、色だけに依存せずアイコン・文言を併用する（5章・12章）。
4. **実装コストを増やさない**：Tailwind v4のデフォルトのスケール・ブレークポイントに乗せ、フォント・アイコンは追加ライセンス費用が発生しないものを選ぶ（6〜8章、13章）。

## 4. Visual Direction

**印象**：「静かで温かい記録帳」。競争心を煽るゲームアプリでも、無機質な業務ツールでもない。称号解除というゲーム的要素はあるが、それは「勝ち負け」ではなく「自分の成長の記録」として演出する。

**トーンのものさし**：
- 上質・洗練 ←────●──→ 親しみ・カジュアル（やや親しみ寄り）
- 静か・落ち着き ←──●────→ 賑やか・刺激的（静か寄り。ただし称号解除の瞬間だけAccentカラーで一時的に華やぐ）
- フラット ←──●────────→ 装飾的（フラット寄り。影より色と余白で表現する）

具体的には、ブランドカラーに「育つ・育てる」を象徴する緑を採用しつつ（5章）、影をほぼ使わずborderで輪郭を作るフラットな構成、Noto Sans JP単一フォントによる素直な組版、装飾的な字間・角丸の作り込みを避けることで、「気合を入れて開くアプリ」ではなく「日々の生活に溶け込む記録帳」という方向性を徹底する。

## 5. Color System

### 5.1 基本方針

ブランドカラーには緑を採用する。子育てを「育つ・育てる」というイメージと重ね、日々の記録が積み重なっていくことを、植物が芽吹き育っていく様子になぞらえる。

TotoOpsでは、一般的な企業向けサービスで見られる重厚な緑や青緑ではなく、育児記録アプリの目的に合う、明るく鮮やかな新緑の若葉色を採用する。配色は次の3点を基準に設計する。

1. **テーマに合う色相を選ぶ**：黄みのある若葉色（およそ103°）を採用し、「深い森」や「企業カラー」ではなく、「芽吹き・新緑」を連想させる方向にする。
2. **明度・彩度をともに上げる**：明度53%・彩度49%の、芽吹いたばかりの葉のような軽やかな緑にする。重厚さや渋みを避け、これから育っていく子どもと同じ「若さ」を色そのもので表す。ただしこの明るさには帰結がある。明るい緑に白文字を乗せると本文コントラストが2.2:1程度しか出ずWCAG AAを満たせないため、**Primaryは「塗り」専用とし、その上に乗せる文字はText Primaryを使う**。あわせて、白背景の上に緑の文字・アイコン・枠線を置く用途には、同じ色相の濃色版**Primary Deep**を用意して役割を分ける（5.2節・5.3節）。緑が2系統あるのは装飾ではなく、この明るさを可読性と両立させるための構成である。
3. **状態色にブランド色相を流用しない**：状態色（Success/Info）は緑ではなくあえて青系に割り当て、「ブランド＝緑」「状態表現＝青」の役割を完全に分離する。ブランドカラーと状態色が同じ色相だと、通常のボタンなのか何かのアラートなのか瞬時に判断できなくなるためである（3章の原則3）。

### 5.2 カラーパレット

| トークン | HEX | 用途 | 採用理由 |
|---|---|---|---|
| **Primary** | `#6EC24C` | 主要CTAの**背景（塗り）**、選択状態、ブランドロゴ色 | 黄みのある若葉色（色相約103°・明度53%・彩度49%）。芽吹いたばかりの新緑を表し、「育つ・成長する」イメージを重厚さではなく明るさで象徴する。**この上に文字を乗せる場合は白ではなくText Primary（`#33302C`）を使う**（白文字では2.2:1でAA不成立） |
| **Primary Hover** | `#5BB53B` | Primaryのホバー・押下時のフィードバック | Primaryを暗く落とすことで押下感を出す。新しい色相を増やさずに状態変化を表現する |
| **Primary Deep** | `#2D7916` | 白背景の上に置く緑の**文字・アイコン・枠線**（Text Button、グローバルナビのアクティブ表示、入力欄のフォーカス枠とフォーカスリング、Primaryボタンの枠線） | Primaryと同じ色相の濃色版（色相約106°・明度28%）。明るいPrimaryは白背景に対して2.2:1しかなく文字色としてAAを満たせないため、可読性を担保する役割だけを担う。色相を共有しているのでブランドの一貫性は崩れない |
| **Primary Subtle** | `#EEFAE5` | 8タイルグリッドの選択済み状態の背景、淡いハイライト、選択中タブの背景 | Primaryの極薄ティント。彩度の高い色を増やさずに「選択中」を示せる。ただし背景との明度差がほとんど無いため、単独で状態を伝えず必ず枠線を併記する（10章 Cards） |
| **Secondary** | `#5B5650` | 補助ボタンの文字・枠線、控えめなラベル、非アクティブなタブの文字色 | 暖色寄りのニュートラルグレー。Primaryと競合せず、「これは補助的な操作である」ことを一目で示す |
| **Accent** | `#D97757` | 称号解除・達成演出（S5/S6モーダル）、連続記録バッジなど、"祝福"の瞬間限定 | 緑と明確に対比する暖かいテラコッタ。競争を煽る金色のトロフィーのような色は避けつつ、「成長（緑）に対するご褒美（暖色）」という対比でRPG的な達成の高揚感を表現する。使用箇所を祝福演出だけに限定し、希少性を保つことで効果を持続させる |
| **Background** | `#FBF9F6` | ページ全体の背景 | 純白よりわずかに温度のある白。単一の温かみある背景色で統一し、実装コストを抑えつつ、生活感とくつろぎを演出する |
| **Surface** | `#FFFFFF` | カード、入力欄、モーダルなどコンテンツが乗る面 | Backgroundとのわずかな明度差で「面」を知覚させる。8.2節の通り、面の境界は原則として影ではなくborderで表現する |
| **Text Primary** | `#33302C` | 本文、見出し | 純黒を避けた温かいダークグレー。長文閲覧・夜間利用での目の疲れを軽減する |
| **Text Secondary** | `#6E6A64` | 補足文、タイムスタンプ、キャプション、プレースホルダー | Text Primaryとの明確な明度差で情報の重要度を伝える |
| **Border** | `#DEDAD3` | 区切り線、入力欄の枠、カードの輪郭 | Backgroundと調和する暖色寄りの控えめなグレー。影を使わずカードの輪郭を示す主手段 |
| **Success** | `#2F6690` | 記録保存成功のトースト、称号条件達成の確認表示 | あえてPrimaryと異なる青系の色相を採用し、「ブランド＝緑」「状態＝青」と役割を完全に分離する |
| **Warning** | `#B4791E` | 記録が数日途切れている旨のやさしいリマインド表示 | 落ち着いたアンバー。警告色にありがちな刺激を抑え、「啓発であり叱責ではない」という製品方針に合わせる |
| **Error** | `#B3435A` | フォームのバリデーションエラー、送信失敗など | 純赤ではなく彩度を落としたローズ寄りの赤。責める印象を避けつつ、エラーとして識別できる色相を保つ |
| **Info** | `#5D82A0` | 補足情報バナー、ツールチップ、プライバシーに関する注記（12章参照） | Successより明度を上げた同系ブルー。状態色ファミリー内で軽重を作りつつ、Primaryの緑からは独立させる |

### 5.3 コントラストの考え方

- Primaryは明るい若葉色のため、**白文字を乗せない**。相対輝度から算出したコントラスト比は概算で次の通りで、いずれもWCAG AAの本文基準（4.5:1）を満たす。
  - CTAボタン：Primary（`#6EC24C`）にText Primary（`#33302C`）＝約5.9:1。ホバー時のPrimary Hover（`#5BB53B`）でも約5.1:1。
  - 白背景に置く緑の文字・アイコン：Primary Deep（`#2D7916`）＝Surface（`#FFFFFF`）に対し約5.4:1、Background（`#FBF9F6`）に対し約5.2:1。
  - グローバルナビ（デスクトップ）のアクティブ項目：Primary Subtle（`#EEFAE5`）の上にPrimary Deep＝約5.0:1。
- Success/Warning/Error/Infoは、Primaryとは異なり白文字での使用を前提に中明度以下のトーンで設計している。
- Text Primary/Secondaryは、Background（`#FBF9F6`）・Surface（`#FFFFFF`）のいずれに対しても十分な明度差を確保している。
- 上記は設計時の概算であり、**実装時には自動コントラストチェッカー（ブラウザDevToolsやLighthouse等）で本文4.5:1／大きな文字・アイコン等の非テキスト要素3:1を必ず最終確認すること**。値の妥当性は理屈の上で担保しているが、最終検証はツールに委ねることで実装コストを抑える。

### 5.4 状態色は色だけに依存しない

Success/Warning/Error/Infoはいずれも、色の変化だけでなく**アイコン＋短い文言**を必ず併記する（12章）。色覚特性によって色の差が分かりにくいユーザーにも状態が伝わるようにするためであり、3章の原則3・11章の全状態に共通するルールでもある。

## 6. Typography

### 6.1 フォントファミリー

単一フォントファミリー「Noto Sans JP」＋システムフォールバックのみを採用する。

```css
--font-sans: "Noto Sans JP", -apple-system, BlinkMacSystemFont, "Hiragino Kaku Gothic ProN", sans-serif;
```

`src/resources/css/app.css` の `@theme` に反映済み（`--font-sans`）。Webフォント本体は Bunny Fonts 経由で `src/resources/views/app.blade.php` が読み込む（Google Fonts と同一の資産を、IPアドレスを記録しないCDNで配信するサービス。`docs/privacy.md` の「個人情報は最小限に」の方針に沿うため直の Google Fonts は使わない）。

和文と欧文で別フォントを使う、あるいは同じフォントの複数ウェイトを別ファイルとして読み込む、といった構成は採用しない。フォントは1系統・Google Fontsで無償配信されているもの1つに絞ることで、ライセンス費用・読み込みコストの両方を最小化する。ウェイトはTailwind標準の3段階（400 / 500 / 700）のみを使い、4章の「フラットで素直な組版」という方向性を支える。

### 6.2 サイズ・行間・ウェイトの階層

| Role | Size | Weight | Line Height | 用途 |
|---|---|---|---|---|
| Display | 28px | 700 | 1.4 | オンボーディング（S2）等、画面内で一度だけ使う特大見出し |
| Heading L | 22px | 700 | 1.4 | 画面タイトル（S3/S12/S13/S7など各画面の見出し） |
| Heading M | 18px | 600 | 1.5 | カード・セクション内の小見出し |
| Body | 16px | 400 | 1.7 | 本文の標準サイズ |
| Body Small | 14px | 400 | 1.6 | タイムスタンプ、補足キャプション、メタ情報（**これ以下のサイズは使わない下限**） |
| Label / Button | 15px | 600 | 1.4 | ボタン内テキスト、フォームラベル、タブ項目 |

- 本文の行間1.7は、和文の可読性を優先した値。行間が詰まりすぎると疲れた状態での読み飛ばしミスにつながるため、やや広めを標準とする。
- 14pxを本文系テキストの下限として明言する。ブランド上の都合で文字を小さくしたい場面があっても、この下限は割り込まない。
- 見出しはウェイト700、本文は400、ボタン・ラベルは600、という3段階だけで階層を作り、色数を階層表現に使わない（3章原則3）。

### 6.3 字間

字間はほぼ0（0〜0.01em）とし、装飾的な広い字間（レターイング）は採用しない。これは4章で述べた「上質・洗練よりやや親しみ寄り」というトーンのものさしに基づく明示的な判断であり、ブティック的な高級感の演出としての広い字間は、TotoOpsが目指す「生活に溶け込む記録帳」という方向性とは合わないため、意図的に採用しない。

### 6.4 改行・折り返し

```css
overflow-wrap: break-word;
```

称号名や育児イベント名など日本語と絵文字・英数字が混在する短い文字列があるため、長い連続文字列でもレイアウトが崩れないよう `overflow-wrap: break-word` を基本設定とする。特別な禁則処理はブラウザデフォルトに委ねる。

## 7. Spacing System

4pxを基準単位とした8段階のスペーシングトークンを定義する。Tailwind v4のデフォルトスケール（4pxグリッド）にそのまま乗せることで、新規の設定追加なしに利用できる。

| Token | Value | 用途 |
|---|---|---|
| space-xs | 4px | アイコンとラベルの間隔など、最小の間隔 |
| space-sm | 8px | コンパクトな内部余白、タグ・バッジ内側 |
| space-md | 16px | 標準的な要素間の間隔、フォーム項目間 |
| space-lg | 24px | カード内側のパディング、コンポーネント間の区切り |
| space-xl | 32px | セクション内側のパディング |
| space-2xl | 48px | セクション間の区切り（モバイル） |
| space-3xl | 64px | セクション間の区切り（デスクトップ）、ページ上部の余白 |

余白は「セクション間（2xl/3xl）＞コンポーネント間（lg/xl）＞要素内（xs/sm/md）」の3階層で一貫させ、場当たり的な値（例：13pxや22pxのような任意の数値）を使わない。

## 8. Layout

### 8.1 コンテンツ幅

- モバイル：画面幅いっぱい（左右 space-md=16px の余白のみ）。
- デスクトップ：メインコンテンツの max-width は **960px**。8タイルグリッド（S3/S9）や集計画面（S12）のグラフのように、横に広がりすぎると視認性が落ちるコンポーネントが多いため、1200pxを超える広いコンテナは採用しない。

### 8.2 面の境界と階層表現

カード・入力欄・リスト行などの「面」の輪郭は、**影ではなく1px solidのBorderで表現することを基本とする**。Backgroundに対してSurfaceがわずかに明るいこと、Borderが控えめに輪郭を引くことの2つで十分に階層を伝えられるため、通常のカードには `box-shadow` を使わない。

影を使うのは、「本当にコンテンツの上に浮いている」要素だけに限定する（11章の状態表現とは別に、以下の2段階のみを定義）。

| Level | Shadow | 用途 |
|---|---|---|
| 0（既定） | なし（borderのみ） | 通常のカード、リスト行、入力欄 |
| 1 | `0 1px 2px rgba(51,48,44,0.06)` | デスクトップでのカードhover、リスト行hover |
| 2 | `0 4px 16px rgba(51,48,44,0.12)` | モーダル（S5/S6/スロット入替モーダル/削除確認モーダル）、フローティングのタブバー、ドロップダウン |

### 8.3 セクション構成

`docs/wireframes.md` の合意済みレイアウトに準拠し、グローバルナビ（記録／履歴／集計／設定）を持つS3・S12・S13・S7では、ナビを除いたメインエリアをspace-xl（32px）の内側パディングで囲む。S2/S4/S8/S9/S10/S11のようなナビを持たない単機能画面は、上部にspace-lg（24px）の余白を置いた縦1カラム構成を基本とする。

## 9. Responsive Design

**モバイルファースト**を明言する。理由は3点：

1. `docs/wireframes.md` で定義済みのナビゲーションが、モバイルでは画面下部のタブバー、デスクトップでは左サイドバーという構成であり、モバイルの操作性が起点になっている。
2. 2章で述べた通り、深夜の授乳・おむつ替え直後などスマートフォンでの片手操作が主利用シーンである。
3. CLAUDE.mdに記載の通り、将来的にPWA化する計画があり、モバイルでの体験がプロダクトの核となる。

### ブレークポイント

単一のブレークポイントのみを定義する：**768px**（Tailwind v4のデフォルト `md:` をそのまま使用し、新規設定を追加しない）。

| 状態 | 幅 | ナビゲーション | グリッド |
|---|---|---|---|
| Mobile（既定） | 〜767px | 画面下部固定タブバー（4項目） | 8タイルグリッドは4列×2行 |
| Desktop | 768px〜 | 左サイドバー（4項目、同一ラベル） | 8タイルグリッドは4列×2行のまま最大幅を制限して中央寄せ |

複数段階のブレークポイント（タブレット専用レイアウト等）は設けない。MVPの画面数・複雑度に対しては過剰であり、検証・保守のコストが実装効果に見合わないと判断したため。将来的に画面が増えた段階で必要になれば追加を検討する。

### タッチターゲット

すべてのタップ可能要素は最小 **44×44px** を確保する（8タイルグリッドの各タイル、タブバー項目、リスト行の右端メニュー等）。

## 10. Components

### Buttons

| Variant | 背景 | 文字色 | 枠線 | 角丸 | Padding | 用途 |
|---|---|---|---|---|---|---|
| Primary | `#6EC24C` | `#33302C` | 1px solid `#2D7916` | 12px | 12px 20px | 画面ごとに1つだけ置く主要操作（記録の保存、SSOログイン等） |
| Secondary | 透過 | `#5B5650` | 1px solid `#DEDAD3` | 12px | 12px 20px | 主要操作と並ぶ補助操作（キャンセル、戻る） |
| Text Button | 透過 | `#2D7916` | なし | — | 8px 4px | リスト内の軽い操作（「もっと見る」等） |
| Destructive | 透過 | `#B3435A` | 1px solid `#B3435A` | 12px | 12px 20px | 記録削除など取り消し不能な操作。塗りにはせず、誤タップの被害を抑える |

角丸12pxは、9.5節・7章のスペーシングと同じ4の倍数系列から選び、Primary/Secondaryで統一する。1画面に塗りのボタン（Primary）は原則1つまでとし、「今何をすべきか」が常に一目で分かる状態を保つ。

Primaryボタンだけは、他のボタンと違って**文字色が白ではなくText Primary**であり、**枠線を持つ**点に注意する。どちらも明るい若葉色を塗りに使ったことの帰結で、前者は白文字ではAAを満たせないため（5.3節）、後者はPrimaryの塗りがBackgroundに対して2.1:1しかなく、塗りだけではボタンの輪郭が温かい白背景に溶けてしまうため（非テキスト要素の3:1を満たさない）。Primary Deepの枠線を1px引くことで輪郭を確定させ、3章の原則1「主要操作と補助操作を塗り分けで明確に区別する」を保つ。

### Forms

- 入力欄：背景 `#FFFFFF`、枠線 1px solid `#DEDAD3`、角丸6px、内側パディング 12px 16px、フォントサイズ16px（モバイルでの自動ズームを避けるため16px未満にしない）。
- フォーカス時：枠線を `#2D7916` に変え、さらに外側に `0 0 0 3px rgba(45,121,22,0.25)` のフォーカスリングを表示する（12章）。
- ラベルは入力欄の上に常設表示し、プレースホルダーだけに頼らない（プレースホルダーは消えると入力内容が何だったか分からなくなるため補助情報に留める）。
- 任意項目（年代・子の年齢層など）には必ず「未回答」を選べる選択肢を用意し、回答を事実上強制しない（`docs/privacy.md` 準拠）。

### Cards

- 背景 `#FFFFFF`、枠線 1px solid `#DEDAD3`、角丸16px、内側パディング space-lg（24px）。
- 影は既定では使わない（8.2節）。デスクトップでのhoverのみLevel 1の影を付け、操作可能であることを示す。
- 8タイルグリッド（S3/S9）のタイルはカードの一種だが、看板コンポーネントとして角丸を**20px**とやや大きくし、他のカードより「押しやすく特別な場所」であることを視覚的に示す。選択済みタイルは背景をPrimary Subtle（`#EEFAE5`）にし、**あわせて枠線を2px solid `#2D7916` に変える**。Primary SubtleはBackground・Surfaceに対して1.1:1未満の明度差しかなく、ティントだけでは選択状態を判別できないため、枠線を必ず併用する（3章の原則3・5.4節と同じ理由）。

### Tables and Lists

- 履歴（S13）は日付グループ見出し＋行のリスト形式で統一する。表（Table）は集計画面（S12）の内訳表示など、行×列の比較が必要な場合のみに限定して使用する。
- リスト行の右端は、編集可能な行では「…」ケバブメニュー、画面遷移だけの行では「›」シェブロン、と役割ごとに記号を統一し、`docs/wireframes.md` の既存合意と一致させる。
- 行の区切りは1px solid `#DEDAD3` の下線のみとし、行ごとに背景色を変える縞模様（ゼブラストライプ）は採用しない（情報密度を上げすぎないため）。

### Navigation

- モバイル：画面下部固定のタブバー。高さ56px、背景 `#FFFFFF`、上端に1px solid `#DEDAD3`、Level 2の影。アクティブ項目はアイコン・ラベルともに `#2D7916`、非アクティブは `#6E6A64`。
- デスクトップ：左サイドバー、幅220px、背景 `#FFFFFF`、右端に1px solid `#DEDAD3`。アクティブ項目の背景に Primary Subtle（`#EEFAE5`）を敷き、文字色を `#2D7916` にする。
- ナビ項目は4つ（記録／履歴／集計／設定）固定。表示するのはS3/S12/S13/S7のみで、モーダルや単機能画面（S2/S4/S8/S9/S10/S11）では非表示にする（`docs/screens.md` 準拠）。

### Dialogs and Notifications

- モーダル（S5称号解除、S6シェア文言、スロット入替、削除確認）：背景を `rgba(51,48,44,0.5)` の半透明オーバーレイで覆い、コンテンツを画面中央に配置。コンテンツ本体は角丸16px、Level 2の影、背景 `#FFFFFF`。
- 称号解除モーダル（S5）は、wireframesで既定の🏅絵文字とAccentカラー（`#D97757`）を使った祝福演出にする。派手な紙吹雪アニメーション等は必須にせず、実装コストとのバランスを優先する。
- トースト通知（保存成功・エラー）は画面下部（モバイル）／右下（デスクトップ）に表示し、Success/Errorの色と対応するアイコン・短い文言を必ず併記する。自動で消えるまでの秒数は十分に長め（目安4秒以上）にし、疲れた状態でも読み切れるようにする。

## 11. States

状態表現は「色」「アイコン」「文言」の3点セットを常に揃える（3章原則3、5.4節）。

### Hover

- ボタン・リンク・カード：Primary系トーンを1段階暗く、または背景にPrimary Subtleを薄く敷く。デスクトップのみ（タッチデバイスでは意味を持たないため実装しない）。

### Focus

- キーボード操作時のフォーカスは、対象要素の外側に `0 0 0 3px rgba(45,121,22,0.25)` のフォーカスリングを表示する。ブラウザ標準のフォーカス表示を消して独自スタイルに差し替える場合も、必ずこのフォーカスリングで代替し、フォーカス表示を完全に消すことはしない（12章）。

### Disabled

- 背景・文字色ともに不透明度を下げる（`opacity: 0.5`）のではなく、Text Secondary（`#6E6A64`）を使った専用の淡いトーンを定義し、`cursor: not-allowed` を付与する。二重送信防止のため、送信中のボタンは disabled 状態にし、ラベルをLoading状態（次項）に切り替える。

### Loading

- ボタン内：ラベルを隠し、そのボタンの文字色と同色の簡易スピナーを中央に表示（Primaryボタンなら `#33302C`）。ボタンサイズは変えない（レイアウトジャンプを防ぐ）。
- 画面全体：スケルトン（要素の形をしたグレーのプレースホルダー、`#DEDAD3` の淡色）を表示し、真っ白な空白のまま待たせない。

### Empty

- `docs/wireframes.md` 既定のパターンに従い、中央にやさしいメッセージ＋「記録する」CTAボタン（Primary）を配置する。「データがありません」のような無機質な文言ではなく、「まだ記録がありません。最初の一歩を記録してみましょう」のような前向きな文言にする（14章）。

### Error

- フォーム内：該当項目の枠線をError色（`#B3435A`）に変え、直下にアイコン＋短い文言でエラー内容を表示する。エラーメッセージは「何が悪いか」ではなく「どう直せばいいか」を主語にする（14章）。
- 通信エラー等の全体エラー：トーストまたはインラインバナーで表示し、Primaryボタンでの再試行導線を必ず添える。

### Success

- 記録保存など：Success色（`#2F6690`）のトーストに✓アイコンと短い文言。派手なアニメーションは付けず、静かに数秒で消える。
- 称号解除など「祝福」の成功は、Success色ではなくAccent色（`#D97757`）を使うモーダル演出（10章 Dialogs参照）とし、通常の保存成功と体験を明確に分ける。

## 12. Accessibility

- **色だけに依存しない**：状態表現・グラフの凡例など、色の差だけで意味を伝える箇所を作らない。必ずアイコンまたはテキストラベルを併記する。
- **コントラスト**：本文は4.5:1以上、大きな文字（18px以上かつ太字、または24px以上）・アイコン等の非テキスト要素は3:1以上を満たす（5.3節）。実装時にツールで最終確認する。
- **フォーカス表示**：11章の通り、キーボード操作時のフォーカスリングを省略しない。`outline: none` を付けるだけでフォーカス表示を消す実装は禁止する。
- **タップ領域**：9章の通り最小44×44px。
- **フォームの任意性**：機微な項目（年代等）は「未回答」を選べるようにし、回答を強制しない（`docs/privacy.md` 準拠）。
- **集計・ランキング表示への注記**：`docs/privacy.md` の方針に基づき、タスク種別ランキング等を表示する画面には「個人・地域を比較・評価するものではない」旨の注記をInfo色（`#5D82A0`）のバナーで添える。

## 13. Icons and Images

現状、アイコン・UIコンポーネントライブラリは未導入（`src/package.json` にも含まれていない）。以下を新規導入する方針とする。

- **育児イベント種別アイコン（17種、8タイルグリッド用）**：絵文字ベースを採用する。`docs/wireframes.md` の称号解除モーダル（S5）で既に🏅絵文字が使われている前例に合わせ、追加のライセンス・デザイン工数なしで温かみのある表現ができるため。
- **UIクロム（ナビ・矢印・ケバブメニュー・チェックマーク等）**：Heroicons（MITライセンス）を採用する。Tailwind CSSエコシステムでの採用実績が多く、線幅・サイズの一貫性が保たれているため。
- **写真・人物画像**：`docs/privacy.md` により子どもの写真は取得しないため、装飾目的の人物写真・ストックフォトは使用しない。イラストを使う場合も、特定の人種・体型・家族構成を暗示しない、抽象度の高い表現に留める。

> この2点（絵文字＋Heroicons）はDESIGN.md作成時点での設計判断であり、`docs/decisions.md` に別途明記された既存の決定と矛盾しないか、実装着手前に確認が必要（16章参照）。

## 14. Content and Wording

ビジュアルと文言のトーンは切り離せない（3章原則2、11章）。

- **叱責しない**：「サボっていませんか」ではなく「最近お休み中みたいですね、無理せずいつでも記録してくださいね」のように、あくまで本人のペースを尊重する文言にする。
- **比較しない**：「〇〇さんより少ないです」のような他者比較を示唆する文言は一切使わない。許容されるのは「全体で一番記録されているのはおむつ交換です」のようなタスク種別の集計のみ。
- **前向きな空状態**：「データなし」ではなく次の一歩を促す文言にする（11章 Empty参照）。
- **エラーは解決志向**：「入力が不正です」ではなく「メールアドレスの形式を確認してください」のように、原因ではなく対処法を主語にする。

## 15. Prohibited Patterns

- 母親や他の父親との比較UI（グラフ、ランキング、達成率の並列表示を含む）。
- 個人単位・都道府県や市区町村などの地域単位のランキング表示。許容されるのは育児タスクの種類ごとの順位のみ。
- 「頑張りすぎアラート」「サボり通知」のような赤い警告バナーや通知音による叱責トーンの表現。
- 色のみで状態（成功／警告／エラー）を伝える表現（アイコン・文言の併記を省略すること）。
- 子どもの氏名・誕生日・写真、利用者の本名、都道府県・市区町村、位置情報をフォーム・表示画面に含めること（`docs/privacy.md` 準拠）。
- キーボードフォーカスの表示を完全に消す実装（`outline: none` のみで代替を用意しない）。
- 影を多用した装飾的な立体表現（8.2節の通り、面の境界は原則border）。
- 広い字間（レターイング）や複数フォントの組み合わせによる装飾的な組版（6章の通り単一フォント・字間ほぼ0を基本とする）。

## 16. 独自性のある設計判断

TotoOpsのデザインは、公開されている一般的なUI/UXの知見を参照しつつ、プロジェクトの目的・想定利用者・機能要件から独自に決定している。

- **色相・トーン**：ブランドカラーには、育児の「育つ・育てる」というテーマに合う若葉色を採用した。重厚さや渋みではなく、芽吹いたばかりの新緑の明るさ（明度53%）を優先し、これから育っていく子どもと同じ「若さ」を色そのもので表している（5.1節）。
- **塗り用と文字用で緑を2系統に分ける**：明るい若葉色は、白文字を乗せることも、白背景の上に文字色として置くこともできない（どちらも約2.2:1）。そのため塗り専用のPrimaryと、白背景に置く文字・アイコン・枠線用のPrimary Deepに役割を分けた。トークンを1つ増やすのは3章の原則4「実装コストを増やさない」と引き換えだが、明度を落として色を渋くする代わりに支払うコストとして許容している（5.1節・5.2節）。
- **状態色の役割分離**：Primaryの緑と、Success/Infoの青系を分離し、ブランド表現と状態通知を混同しない設計にした（5.2節）。
- **単一背景とborder中心の階層表現**：ページ背景を温かみのある単一色で統一し、カードなどの面は影を多用せずborderで区切ることで、静かな記録帳という方向性を保つ（8.2節）。
- **用途別の角丸スケール**：6/12/16/20pxの角丸を用途ごとに使い分け、看板コンポーネントである8タイルグリッドのみ角丸を大きくする固有ルールを設けた（10章 Cards）。
- **簡潔なタイポグラフィ**：単一フォントと3段階のウェイトに限定し、装飾的な字間や複数フォントの組み合わせを避けることで、実装と保守の負担を抑えながら可読性を確保する（6章）。
- **祝福と通常成功の分離**：通常の保存成功はSuccess色、称号解除などの達成演出はAccent色として明確に分け、日常操作と特別な体験の差を表現する（5.2節・11章 Success）。

これらの判断は、TotoOpsの利用状況と心理的安全性を基準に設計したものであり、特定の企業・サービスの外観を再現することを目的としていない。

---

## 未確認事項（実装着手前に確認が必要）

- **アイコン方針（13章）**：絵文字＋Heroiconsという組み合わせは本書作成時点の設計判断であり、`docs/decisions.md` に既存の決定事項がないか未確認。矛盾がある場合は `docs/decisions.md` の記載を優先する。
- **Tailwind `@theme` へのトークン追加（部分的に完了）**：5.2節の色14トークンと6.2節のタイポグラフィ6トークン、および6.1節のフォント設定は `src/resources/css/app.css` の `@theme` に反映済み。角丸（6/12/16/20px）とスペーシングは Tailwind 標準のスケールで表現できるため独自トークンを作っていない。8.2節の影 Level 1/2 は使用箇所（カードhover・モーダル）が未実装のため未定義で、実装時に追加が必要。
- **Heroiconsの追加**：`src/package.json` に依存関係として追加する必要がある（現状未導入）。
